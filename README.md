# MouseClicker

Simple C# auto-clicker with F8 toggle and configurable click interval.

## Features

- F8 toggles auto-clicking on and off.
- Default click interval is 100 ms.
- Custom interval can be set with `--interval-ms`.
- Clicks at the current cursor position.

## Usage

Run with the default interval:

```powershell
MouseClicker.exe